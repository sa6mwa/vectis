# Lua Coverage Matrix

The `vectis` executable is a primary product surface. This matrix tracks the
Lua exposure contract for bundled dependencies and Vectis C SDK workflows. The
developer-facing entry point is [`docs/lua.md`](lua.md).

Coverage states:

- `yes`: implemented and exposed to Lua.
- `partial`: useful Lua coverage exists, but the row still has explicit gaps.
- `planned`: no complete Lua surface yet.
- `n/a`: intentionally not a direct Lua API; explain the reason in notes.

Columns:

- `Native`: thin dependency/module access, preferably preserving upstream naming.
- `DX`: Vectis-owned helper layer for common service workflows.
- `Local`: deterministic local test coverage.
- `Packed`: packed-binary scenario coverage.
- `Live`: opt-in live-provider or hardware/service coverage.

## Dependency Facades

| ID | Surface | Native | DX | Local | Packed | Live | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| dep:lua-runtime | cpkt Lua 5.5 runtime facade | yes | yes | yes | yes | n/a | Runtime is embedded; not a user module. |
| dep:lockdc | liblockdc / lockdc Lua module | yes | partial | yes | yes | n/a | `require("lockdc")` and `vectis.lockd`; JSON passthroughs, embedded bundle config DX, one-shot state save/load, and queue enqueue helpers are documented in `docs/lua-lockd.md`; additional helpers remain only where direct lockdc usage is too noisy. |
| dep:lonejson | LoneJSON Lua module | yes | partial | yes | yes | n/a | Native encode/decode/schema available; Vectis JSON route/downstream DX remains incomplete. |
| dep:pslog | libpslog Lua module | yes | partial | yes | yes | n/a | The dependency-native logging module is preloaded, documented in `docs/lua-pslog.md`, and packed-tested; `vectis.log` adds JSON logger defaults, default fields, and structured Vectis error logging while keeping direct pslog access available. |
| dep:lql | liblql Lua module | yes | n/a | yes | yes | n/a | Dependency-native module is preloaded, documented in `docs/lua-lql.md`, smoke-tested, and packed-tested through `examples/lua/local_data_pipeline.lua`; no Vectis-owned helper is planned until a repeated service workflow crosses Vectis concepts. |
| dep:cai | CAI Lua module | yes | partial | yes | yes | yes | Dependency-native CAI 0.4.0 module is preloaded, documented in `docs/lua-cai.md`, and packed-tested for local constructors, model metadata, tool schemas, response params, registries, MCP handler construction, and MCP client constructor presence; `vectis.cai` adds config/client/agent/error DX while preserving CAI as the primary SDK. C `vectis_cai_worker_service`, C request/reply envelope helpers, Lua `app:cai_worker_service()`, and `vectis.cai_worker` helpers are implemented for one-shot mailbox work with deterministic service-only, supervised, and packed example coverage. `examples/lua/cai_live_service_adapter.lua` covers Lua `app:mcp()` route-adapter registration, supervised readiness probing, packed execution, and opt-in live provider validation through `make test-cai-live` or `make prerelease-live` with `VECTIS_LUA_CAI_LIVE=1`. |
| dep:libmdf | libmdf Lua module | yes | partial | yes | yes | n/a | Dependency-native module is preloaded, documented in `docs/lua-libmdf.md`, and packed-tested; `vectis.terminal` adds Markdown render DX while keeping direct libmdf access available. |
| dep:softline | softline Lua module | yes | partial | yes | yes | n/a | Dependency-native module is preloaded, documented in `docs/lua-softline.md`, and packed-tested; `vectis.terminal` adds bounded editor construction while keeping direct softline access available. |
| dep:curl | libcurl Lua facade | yes | partial | yes | yes | planned | `curl`, `vectis.http`, `vectis.webdav`, `vectis.mqtt`, `vectis.smtp`, and `vectis.curl_worker`; generic protocols are available through option tables, while the managed curl worker currently exposes the C HTTP mailbox envelope and service registration surface. |
| dep:openssl | OpenSSL | yes | partial | yes | yes | n/a | `openssl` exposes version, SHA-256, general EVP digest/HMAC, hex/Base64 codecs, PEM-backed signing/verification, and CSPRNG helpers; `vectis.cert` owns certificate workflows. |
| dep:libssh2 | libssh2 | planned | partial | yes | partial | yes | `vectis.ssh.open` exposes the public C SSH receiver to Lua; `vectis.ssh.exec`, libssh2-backed SFTP/SCP file upload/download, one-shot SFTP filesystem operations, and stateful SFTP session/file/directory receivers exist; named `SFTP_OPEN_*` flags are exposed; packed SSH command and SCP validation coverage exists; opt-in SSH/SFTP e2e covers live handles plus known_hosts and SHA-256 host-key fingerprint pinning; lower-level dependency-native channels remain intentionally outside the Vectis-owned service surface. |
| dep:libxml2 | libxml2/XML | partial | partial | yes | yes | n/a | `vectis.xml` exposes XML-to-LoneJSON parsing and LoneJSON-to-XML materialized Lua serialization backed by C `lc_sink` writer support; dependency-native DOM/libxml2 remains intentionally unexposed. |
| dep:dsv | Vectis DSV/CSV/TSV helpers | yes | yes | yes | yes | n/a | `vectis.dsv` exposes materialized parse, typed source-to-callback iteration for dynamic and fixed-capacity string fields, spill, row serialization, packed execution coverage, and streaming `app:dsv` upload row routes. |
| dep:opcua | cpkt-opcua | yes | n/a | yes | yes | yes | `require("opcua")` covers the relevant dependency-native cpkt C89 client/server surface: client/foundation plus client endpoint and FindServers discovery helpers, client/server default security byte-material configuration, server lifecycle, node IDs, expanded node IDs, scalar and array values, data values, client raw history reads, node-model, reference, deletion, common attributes, array-dimension attributes, materialized browse/page results, browse-path translation, method registration/metadata/sync client calls, server access-control callbacks, async client read/write/browse/method/node-create callbacks, server PubSub/MQTT graph wrappers, client subscription lifecycle, value-monitor callbacks, compact event callbacks, selected event-field callbacks, full client/server array read/range families, client common attribute reads/writes, client type/view/reference remote node-management, and prepared/triggered event helpers with normal and packed local e2e against deterministic C and Lua-owned test servers; `app:opcua_server_service` registers callback-free Lua-created `opcua.server` handles under the Vectis managed-service lifecycle and `app:opcua_server_service_states` exposes copied state diagnostics; a surface audit enforces that only documented native-pointer exclusions remain, and `VECTIS_OPCUA_PUBSUB_LIVE=1 make test-opcua-pubsub-live` validates live PubSub/MQTT broker setup against the local MQTT integration service. No Vectis-owned OPC UA DX helper layer is planned beyond lifecycle registration and `vectis.libs.opcua`. |
| dep:sus | cpkt SUS / whisper | yes | partial | yes | yes | yes | Dependency-native metadata, catalog lookup, path/cache open error handling, model handles, offline cache status callbacks, process-wide log sink configuration, model-created transcriber handles, PCM transcription methods, segmented decoder/VOX methods, and transcriber callback registration are exposed, documented in `docs/lua-sus.md`, and packed-tested; managed `vectis_sus_worker_service`, C PCM/file transcription envelope helpers, Lua `vectis.sus_worker` helpers, and `app:sus_worker_service()` are implemented for bounded PCM/file transcription over copied mailbox events with deterministic model-free envelope/lifecycle coverage and opt-in loaded-model execution. `make test-sus-audio-live` covers loaded-model PCM, decoder/VOX segmented paths, materialized text, and abort callback propagation when a local model path or explicit cache opt-in is supplied, while `make test-sus-audio-hardening` performs the stronger cached-model transcription check in the hardening gate. |
| dep:audio | cpkt audio / miniaudio | yes | partial | yes | yes | yes | Dependency-native constants, result strings, capability checks, decoder file/URL/callback open, encoder file/callback open, callback reader/writer, VOX, PTT, and capture/playback receiver shells are exposed, documented in `docs/lua-audio.md`, and packed-tested; managed `vectis_audio_worker_service`, C decode/encode/VOX envelope helpers, Lua `vectis.audio_worker` helpers, and `app:audio_worker_service()` are implemented for bounded file decode/encode plus true VOX state/segment mailbox events with C unit and Lua example coverage. Capture/playback devices and SUS loaded-model audio paths remain opt-in live gates. |
| dep:nghttp2 | nghttp2 | n/a | n/a | yes | yes | n/a | Transport dependency behind curl/Kore; no direct app-facing Lua API expected unless HTTP/2 controls become product surface. |
| dep:zlib | zlib | yes | n/a | yes | yes | n/a | `require("zlib")` exposes buffered string deflate/inflate, gzip/gunzip, auto-decompress, file-backed bounded transforms, version, explicit output limits, smoke coverage, and packed local data pipeline example coverage; no Vectis-owned compression helper is planned until a repeated workflow crosses Vectis HTTP, pack, or asset concepts. |
| dep:miniaudio | miniaudio | yes | planned | yes | yes | yes | Covered with `dep:audio`; capture/playback device helpers are exposed with opt-in live-device coverage. |

## Vectis Workflow Facades

| ID | Surface | Native | DX | Local | Packed | Live | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| workflow:server-runtime | Kore-backed Lua server lifecycle | n/a | partial | yes | yes | n/a | `vectis.app.new/start/stop/close`, fixed routes, buffered Lua callback routes, route groups, buffered before/after hooks, production guardrails including listener backlog/request-limit/request-budget/HSTS/WebSocket limit/Server-header/access-log/Kore-curl/Kore-quiet/pretty-error/worker-death controls, `app:consumer_service`, `app:opcua_server_service`, `app:curl_worker_service`, and `app:cai_worker_service` with copied lifecycle state accessors; `vectis.kore` exposes Kore runtime availability, runtime model, worker/WebSocket constants, and embedded-versus-standalone fallback behavior without replacing `vectis.app`. |
| workflow:routes-json | JSON/API route helpers | n/a | partial | yes | yes | n/a | Fixed `app:json`, `app:auth_json`, `app:text`, `app:redirect`, and buffered `app:route`/`app:group` callbacks with request metadata/body, response tables, file responses, file-backed `spooled_source` responses, hooks, and native/callback auth guards. |
| workflow:rest | Buffered JSON REST route and client DX | n/a | yes | yes | yes | n/a | `vectis.rest` wraps buffered server routes/groups with JSON decode/validation/error responses and wraps `vectis.http` with a base-URL JSON client. |
| workflow:routes-streaming | Request/response streaming route helpers | n/a | yes | yes | yes | n/a | `app:upload` provides true request-body streaming through a bounded upload reader and Lua chunk callbacks; `app:route` supports live callback-backed `stream_source` responses and explicit file-backed `spooled_source` responses; `app:sse` provides live SSE framing; the packable Lua API example exercises stream, SSE, and upload routes. |
| workflow:status-errors | Status/error metadata helpers | n/a | yes | yes | yes | n/a | `vectis.status` mirrors public Vectis status and error-source constants for pure-Lua helpers; curl-backed HTTP/WebDAV/MQTT/SMTP errors, dependency-native cpkt-backed audio/SUS/OPC UA errors, and packed embedded helper errors carry status/source metadata plus dependency diagnostics where applicable. |
| workflow:libs | Bundled dependency namespace | yes | yes | yes | yes | n/a | `vectis.libs` aliases `lockdc`, `lonejson`, `pslog`, `lql`, `cai`, `libmdf`, `softline`, `curl`, `opcua`, `openssl`, `zlib`, `audio`, and `sus` to their direct `require(...)` module tables; it does not wrap, replace, or hide the individual dependency modules. |
| workflow:logging | Lua service logging | yes | partial | yes | yes | n/a | `vectis.log` wraps `pslog` logger creation and structured Vectis error metadata; app-owned managed services inherit the app logger for lifecycle diagnostics by default, C configs can override with a borrowed `pslog_logger *`, and Lua service registrations can opt out with `logger_disabled`. |
| workflow:static-assets | Static files and embedded assets | n/a | yes | yes | yes | n/a | Disk static file/directory mounts infer content types from file extensions unless explicitly overridden; embedded read-only, extract, and packed scenarios exist; `vectis.embedded` is a direct preloaded module and top-level `vectis.embedded` alias; packed embedded helper failures return structured Vectis errors. |
| workflow:webdav-server | WebDAV server mounts | n/a | partial | yes | yes | n/a | `app:webdav` covers Vectis-managed mutable storage and direct mutable disk `root_dir` mounts with native/callback auth; `app:webdav_embedded` covers read-only packed asset mounts; packed embedded-site mutable flow exists. |
| workflow:webdav-client | WebDAV client helpers | yes | yes | yes | yes | n/a | `vectis.webdav` wraps curl for PROPFIND, MKCOL, GET, PUT, COPY, MOVE, DELETE, auth/depth/destination headers, structured Vectis/curl errors, and file-backed transfer. |
| workflow:metrics | Server metrics dashboard and JSON snapshot | n/a | yes | yes | yes | n/a | `app:metrics` enables the C-owned opt-in metrics dashboard and JSON routes, shares native/callback route auth providers, and supports optional lockdc/pouch snapshot persistence. |
| workflow:auth | Native auth, OAuth2/OIDC, WebDAV keys | n/a | yes | yes | yes | yes | Core auth functions, Basic Authorization formatting, native/callback providers, auth routes, email tokens, OAuth2/OIDC flows, WebDAV-key issuance, and `vectis.auth.browser_flow` route/provider/WebDAV-key DX are exposed, documented, and covered by Lua provider/email-token/OAuth/browser-flow contract tests. |
| workflow:certs | Certificate/key/CSR/CA workflows | n/a | partial | yes | yes | n/a | Private-key, CSR, bundle generation, source-backed C `vectis_cert_inspect_bundle()` metadata inspection, Lua bundle inspection, bundle validation, cert/key pair validation, CA bundles, CA-signed cert/key pairs, Lua server TLS path/PEM/client-CA config, TLS protocol/cipher controls, and restart-based file/source key-material refresh exist. Lower-level chain/path-building diagnostics remain outside the current Vectis-owned DX. |
| workflow:http-client | Downstream HTTP client | yes | yes | yes | yes | planned | JSON helpers including OPTIONS, simple non-JSON verbs including HEAD and OPTIONS, form bodies, multipart, file upload/download presets, retry/proxy/TLS pass-through, reusable client defaults, and structured Vectis/curl errors exist. |
| workflow:sftp-curl | curl-backed SFTP | yes | yes | yes | yes | n/a | Covered through `vectis.http.sftp_download/upload`. |
| workflow:ssh-exec | SSH command execution | planned | yes | yes | yes | n/a | `vectis.ssh.exec` and reusable `vectis.ssh.open(...):exec(...)` receiver calls exist; packed e2e covers command execution, known_hosts rejection, and SHA-256 host-key fingerprint pinning; lower-level dependency-native libssh2 channels remain intentionally outside the Vectis-owned service surface. |
| workflow:scp | SCP upload/download | planned | yes | yes | yes | n/a | `vectis.ssh.scp_upload_file` and `scp_download_file` expose libssh2-backed file transfer, with packed validation coverage; dependency-native libssh2 channel/session control remains absent. |
| workflow:sftp-libssh2 | libssh2-backed SFTP | planned | yes | yes | yes | yes | `vectis.ssh.sftp_upload_file`, `sftp_download_file`, `sftp_stat`, `sftp_mkdir`, `sftp_remove`, `sftp_rmdir`, `sftp_rename`, `sftp_chmod`, `sftp_open`, named `SFTP_OPEN_*` flags, and `vectis.ssh.open(...):sftp_*` expose one-shot plus reusable SSH and stateful SFTP open/read/write/stat/directory iteration handles, including opt-in SSH/SFTP e2e coverage. |
| workflow:mqtt | MQTT publish workflows | yes | yes | yes | yes | planned | `vectis.mqtt.publish` wraps curl upload mode for MQTT publish with structured Vectis/curl errors and packed validation coverage; live broker coverage remains opt-in. |
| workflow:smtp | SMTP send workflows | yes | yes | yes | yes | planned | `vectis.smtp.send` wraps curl SMTP upload with memory and file-backed payloads plus structured Vectis/curl errors; packed validation coverage exists and live SMTP coverage remains opt-in. |
| workflow:xml | XML parse/serialize workflows | partial | partial | yes | yes | n/a | `vectis.xml.parse()` materializes a table, `parse_record()` returns a Lua-owned LoneJSON record, and `serialize()` materializes XML from a table or record; C `vectis_xml_write_lonejson()` streams mapped records to an `lc_sink` and `vectis_xml_lonejson_to_bytes()` materializes owned bytes. |
| workflow:dsv | CSV/TSV/DSV parse/serialize workflows | yes | yes | yes | yes | n/a | Lua covers memory/path parse, typed row callbacks for dynamic and fixed-capacity string fields, custom delimiters, comments, spill, serialization, packed embedded asset parsing, and streaming upload route-row handling through `app:dsv`. |
| workflow:lockd-state | lockd document/state workflows | yes | partial | yes | yes | n/a | Direct lockdc access plus `vectis.lockd` config/JSON helpers, `load_json`, `save_json`, and `with_acquired_lease`; public examples use the helper layer; richer document/query helper coverage remains only where direct lockdc usage is too noisy. |
| workflow:lockd-queue | lockd queue and consumer workflows | yes | partial | yes | yes | n/a | Direct lockdc access plus `vectis.lockd.enqueue_json` and `with_dequeued_json`; public examples use the helper layer with explicit ack; retry-oriented helper DX remains only where direct lockdc usage is too noisy. |
| workflow:server-consumer | Same-process server plus lockd consumer | n/a | yes | yes | yes | n/a | Scenario coverage exists. |
| workflow:mailbox | In-process C-owned service handoff and Lua owner-state pumping | yes | yes | yes | yes | n/a | `vectis_mailbox` covers bounded copied events, close behavior, request/reply correlation, stats snapshots, `vectis_mailbox_broker` route-local request/reply cleanup, the C-side typed route event adapter, the C-side typed liblockdc consumer mailbox adapter, and the C-side typed OPC UA monitor mailbox adapter; `vectis.mailbox` covers publish, next, request, reply, broker construction/request timeout cleanup, depth, stats, close, bounded `pump()`, and pump callback failure counters in Lua smoke tests, documented in `docs/lua-mailbox.md` and `docs/concurrency-dx.md`. Deterministic C and Lua examples cover route-style request/reply, owner-state pumping, OPC UA/lockd-style C handoff, and OPC UA monitor callback-to-mailbox handoff; WebDAV live typed adapter remains separate follow-up work only if concrete WebDAV code proves generic byte payloads are too noisy. |
| workflow:opcua-client | OPC UA client workflows | yes | n/a | yes | yes | n/a | Local server-backed normal and packed e2e exists for endpoint and FindServers discovery, sync read/write, async read/write, async browse, async node creation, data-value reads, raw history-read structured behavior, full array-family read/range coverage, array-dimension attributes, index-range writes, materialized browse/page results, method metadata reads, sync and async method calls, browse-path translation, subscription lifecycle, value-monitor callbacks, compact event callbacks, selected event-field callbacks, callback-error propagation through `client:iterate()`, basic remote object/variable creation/deletion, common attribute reads/writes, type/view/reference remote node-management, expanded-node references, and connection lifecycle; the cpkt C89 header currently exposes no client PubSub symbols and the surface audit fails if that changes without Lua coverage. |
| workflow:opcua-server | OPC UA server workflows | yes | n/a | yes | yes | yes | Server lifecycle/value/node-model/reference/common-attribute APIs, default security byte-material configuration, data values, array-dimension attributes, materialized browse/page results, method registration/callback retention, method metadata reads, browse-path translation, full array value/read families, numeric range writes, prepared event helpers, disabled PubSub/MQTT graph wrappers, and opt-in live PubSub/MQTT broker validation are covered; native-pointer surfaces are documented C-only exclusions. |
| workflow:opcua-async | OPC UA async/subscription/PubSub workflows | yes | n/a | yes | yes | yes | Callback and event-loop ownership contract is documented in `docs/lua-opcua.md`; async client read/write/browse/method/node-create calls, client subscription lifecycle, value-monitor callbacks, compact event callbacks, selected event-field callbacks, disabled server PubSub/MQTT graph workflows, and opt-in live PubSub/MQTT broker validation are implemented. |
| workflow:cai | AI/client/tool/MCP workflows | yes | partial | yes | yes | yes | Dependency-native CAI local workflows are packed-tested through `examples/lua/cai_local.lua`; `vectis.cai` covers normalized service config, owned/borrowed client and agent helpers, common CAI constructor aliases including `mcp_client`, one-shot response helper setup, and structured CAI error decoration. Public C adapters cover true streaming request-body to `cai_source`, CAI output to HTTP response/lockd/file sinks, error mapping, logger inheritance, CAI MCP handler mounting, and C `vectis_cai_worker_service` plus C request/reply envelope helpers for embedders. Lua `app:mcp()` builds a C-owned CAI registry and dispatches tool calls through buffered Kore route-domain Lua callbacks; `examples/lua/cai_live_service_adapter.lua` covers that route-adapter workflow plus opt-in live provider validation; Lua `vectis.cai_worker.request`/`vectis.cai_worker.decode_reply` plus `app:cai_worker_service()` cover supervised one-shot CAI work without borrowing Lua callbacks into worker services. MCP client support remains upstream CAI-owned and is exposed through the CAI Lua facade. |
| workflow:sus | Whisper/transcription workflows | yes | partial | yes | yes | yes | Catalog, cache-control paths, model-created transcribers, PCM transcription methods, audio decoder/VOX segmented methods, callback registration, packed example coverage, opt-in live/local loaded-model tests, and hardening-only cached model/audio transcript validation are exposed. Managed worker coverage for `vectis.sus.transcribe_pcm` and `vectis.sus.transcribe_file` plus `app:sus_worker_service()` is implemented for copied mailbox events and runtime-domain model/transcriber ownership; loaded-model worker execution remains opt-in because deterministic tests do not commit a Whisper model fixture. |
| workflow:audio | Audio capture/playback/processing workflows | yes | partial | yes | yes | yes | File/callback decode/encode, VOX/PTT, capture/playback receiver shells, packed example coverage, opt-in live device validation, and opt-in SUS loaded-model audio path validation are exposed. Managed worker coverage for `vectis.audio.decode`, `vectis.audio.encode`, and `vectis.audio.vox` plus `app:audio_worker_service()` is implemented for bounded copied mailbox events and runtime-domain audio work; VOX emits copied `vectis.audio.vox.state` and `vectis.audio.vox.segment` events through a service event mailbox rather than a faux streaming materialized reply. |
| workflow:terminal-agent | libmdf/softline terminal and Agent Smith workflows | yes | partial | yes | yes | n/a | `vectis.terminal` covers Markdown render and editor construction DX; Agent Smith-specific workflows remain future work. |
| workflow:pack | Pack/self-contained binary workflows | n/a | yes | yes | yes | n/a | Linux `vectis -a pack` and packed Lua e2e exist; Darwin rejects pack before creating an artifact because Mach-O modification invalidates its required code signature. |
| workflow:totp-qr | TOTP and QR helpers | n/a | yes | yes | yes | n/a | Exposed under `vectis.auth.totp` and `vectis.auth.qr`. |
| workflow:openapi | OpenAPI generation | n/a | yes | yes | yes | n/a | `app:openapi_doc()` attaches route metadata, route helpers accept `openapi` metadata tables, and `app:openapi()` generates JSON/YAML using LoneJSON schemas retained by the server; the packable Lua API example serves `/openapi.json`. |

## Completion Rules

- New bundled dependencies that are useful from app code must add a `dep:*`
  row in this file and an entry in `docs/lua.md` before release.
- New Vectis C SDK workflows must add a `workflow:*` row before release.
- New Lua module documentation must be linked from `docs/lua.md`.
- A `planned` or `partial` row is acceptable only when the Notes cell states
  the concrete missing surface or ownership decision.
- Local deterministic tests are required when a row moves from `planned` to
  `partial` or `yes`, unless the Notes cell documents why the surface is
  live-provider-only.
- Packed tests are required for workflow helpers intended to work in
  self-contained Linux `vectis -a pack` applications.
